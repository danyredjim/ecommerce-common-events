apiVersion: v1
kind: Service
metadata:
  name: ecommerce-common-events
spec:
  type: NodePort
  selector:
    app: ecommerce-common-events
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007